sudo: true
language: python
python:
  - "3.6"

services:
  - docker

install:
  - pip install -r requirements/ci.txt

script:
  - make test

after_success:
  - make build-production-image publish-image